extends ../layout

block content
    .row
        each item in novel
            .col-12.col-md-6.col-lg-3.mt-2
                a(href="")
                    .card(style='width: 18rem;')
                        img.card-img-top(src='/uploads/'+item.image alt='Card image cap',height="200px")
                        .card-body
                            h5.card-title.text-center=item.name
                        
            
